function multiplica(valor1: number, valor2: number): number {
    return valor1 + valor2;
}

console.log(multiplica(10, 10));

function saudação(nome: string): string {
    return `Olá ${nome}`;
}

console.log(saudação('Gabriel'));
